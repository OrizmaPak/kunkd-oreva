declare enum Sizes {
    SMALL = "small",
    MEDIUM = "medium",
    LARGE = "large"
}
type Size = `${Sizes}`;
interface SvgProps {
    size: Size;
}
declare const iconSizes: {
    small: number;
    medium: number;
    large: number;
};
declare const icons: {
    play(size: Size): string;
    pause(size: Size): string;
    stop(size: Size): string;
    replay(size: Size): string;
    volumeDown(size: Size): string;
    volumeOff(size: Size): string;
    volumeUp(size: Size): string;
};
export { Sizes, iconSizes, icons };
export type { SvgProps };
